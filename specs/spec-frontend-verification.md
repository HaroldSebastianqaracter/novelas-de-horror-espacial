# Verificación — Frontend v1

Plan de verificación de [spec-frontend.md](spec-frontend.md). Métodos y etiquetas según [docs/validators.md](../docs/validators.md).

Actualizado el 24 de septiembre de 2026, con las filas 14 a 21 de la lectura de la entrega (spec 0.2). Las rutas de evidencia son relativas a `src/frontend/`.

## Cómo leer la tabla

Igual que [spec3-verification.md](spec3-verification.md):

- **Punto ciego:** lo que el método no ve aunque pase.
- **¿Solitario?:** si la propiedad descansa en un solo método.
- **Estado:** `implementado`, `pendiente` o `fallando`.

Aquí no hay datos autodeclarados por un agente: el frontend solo pinta lo que dice la API, así que la regla 3 no aplica.

| # | Propiedad | Requisito | Método | Tag | Punto ciego | ¿Solitario? | Evidencia | Estado |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | Los tipos generados coinciden con el snapshot del contrato | RF-FE-API-01 | `npm run tipos` y `git diff --exit-code` sobre el fichero generado | `A` | Lo que la API devuelve y no declara (el `payload` sin esquema, el cuerpo del `422`) | No: la fila 2 cubre los huecos | `npm run tipos:comprobar` | implementado |
| 2 | Los ficheros de deuda (`brief.ts`, `reglas.ts`) coinciden con su fuente en Python | RF-FE-API-04 | Test que compara `reglas.ts` (uniones, resoluciones y conjuntos de estados) y los límites de `brief.ts` con un JSON exportado desde `compartido/tipos.py`, `orquestador/estados.py` y `compartido/brief.py` | `T` | Solo compara lo que se exporta; una regla nueva en Python que no se exporte pasa sin avisar | **Sí**: se retira cuando la API publique lo que falta (sección 5) | `src/compartido/api/deuda.test.ts` | pendiente |
| 3 | Cada uno de los nueve estados cae en su columna, y una novela sin ejecución en «En espera» | RF-FE-TAB-01 | Test unitario parametrizado sobre la función de agrupación | `T` | Un estado nuevo en el backend: la API lo tipa como `string`, así que ni el test ni el compilador lo ven. Se pinta tal cual, con aviso (RF-FE-API-04) | Sí, hasta que la API declare los enumerados; entonces un `switch` exhaustivo lo refuerza | `src/funcionalidades/ejecucion/columnas.test.ts` | implementado |
| 4 | Soltar una tarjeta no la cambia de columna hasta que la ejecución lo confirma, y un rechazo la deja donde estaba con su motivo | RF-FE-TAB-03, RF-FE-DAT-05 | Test de integración con MSW por el menú (intención pendiente, después hecha o rechazada) + test unitario de qué pide soltar en cada columna | `T` | El arrastre de dnd-kit no se ejecuta en jsdom: que soltar llame a esa lógica solo se ha comprobado a mano con Playwright (23-09-2026) | No: la fila 11 lo mira a mano | `src/funcionalidades/ejecucion/intenciones.test.tsx`, `acciones.test.ts` | implementado |
| 5 | Solo se ofrecen las intenciones válidas para el estado y las acciones válidas para el tipo de parada | RF-FE-TAB-03, RF-FE-NOV-01, RF-FE-PAR-02 | Test parametrizado estado × acción y tipo × acción | `T` | Lo que valida el worker además del estado (puertas vigentes para `relanzar`); eso llega como rechazo | No: el rechazo del worker es la segunda barrera | `src/funcionalidades/ejecucion/acciones.test.ts` (estado × acción del tablero), `AlertaParada.test.tsx` (tipo de parada × acción, confirmación en dos pasos y avisos de RF-FE-PAR-04) | implementado |
| 6 | Un evento SSE invalida y no escribe; al reconectar se reconsulta todo | RF-FE-DAT-02, RF-FE-DAT-04 | Test de integración: SSE simulado que se corta, y comprobación de las consultas relanzadas y del `Last-Event-ID` al reconectar, más tests del lector de `text/event-stream` | `T` | Suspensiones largas del equipo y proxies que cortan el stream en silencio. Que el SSE real atraviese el proxy de Vite se comprobó en la demostración de la fila 12 | No: el sondeo de RF-FE-DAT-03 cubre el silencio | `src/compartido/api/eventos.test.tsx` | implementado |
| 7 | Un `422 brief_incompleto` lleva al paso de cada campo señalado y lo marca con el mensaje del servidor | RF-FE-BRF-04 | Test de integración con MSW, con un faltante y una contradicción reales (edad frente a intensidad) | `T` | Un código de campo nuevo en el backend que el formulario no sepa ubicar: cae en un aviso general del paso 5 | Sí | `src/funcionalidades/novela/CrearNovela.test.tsx` | implementado |
| 8 | El formulario nunca envía `texto_libre`, `codigo` ni `cita` | RF-FE-BRF-02, RF-FE-BRF-03 | Test de `aBrief` sobre el JSON que se envía, más el cuerpo real del `POST` en el flujo completo | `T` | — | Sí | `src/funcionalidades/novela/CrearNovela.test.tsx` | implementado |
| 9 | Ninguna pantalla tiene violaciones de accesibilidad automáticas | RF-FE-VIS-03 | axe en cada test de pantalla | `T` | Lo que axe no mide: orden de foco con sentido y claridad de los anuncios | No: la fila 11 | `src/pruebas/accesibilidad.ts`, llamado desde el test de cada pantalla (las seis pantallas y los cinco pasos del brief) | implementado |
| 10 | Ningún componente escribe un color literal | RF-FE-VIS-01 | Regla de lint (búsqueda de `#hex`, `rgb(` y `hsl(` fuera de `tokens.css`) | `A` | Colores metidos en SVG o en Three.js | Sí | `eslint.config.js` | pendiente |
| 11 | Las pantallas reproducen la propuesta B, se manejan por teclado y el arrastre se entiende | Decisión 2, RF-FE-TAB-05, RF-FE-VIS-04 | Revisión con el navegador contra el prototipo, en escritorio, a 768 px y en móvil | `I` | Juicio de una persona; no se repite solo | Sí | Capturas en la entrada del registro de iteraciones | pendiente |
| 12 | El flujo completo funciona contra el backend real: crear desde el brief de ejemplo, arrancar, ver avanzar y parar | 3.2 a 3.8, RF-FE-DAT-06 | Demostración con Playwright contra la API y el worker reales, con el puerto falso y una base temporal, por el proxy de Vite (`NOVELAS_API`). Probado el 23-09: el `422` real llevado a su campo, crear, arrancar, el SSE por el proxy, relanzar desde un capítulo, una parada de oficio resuelta por el worker, el lector y el tablero general | `D` | El puerto falso termina una novela en unos dos segundos, así que ni los estados intermedios en pantalla ni **parar** se pudieron ver contra el backend real: los cubren los mocks (filas 4 y 6). Tampoco reproduce el coste y el ritmo de Claude Code. La parada se abrió a mano con `fallo.abrir_parada` porque el puerto falso no provoca ninguna | Sí | Entrada 22 del registro de iteraciones | implementado |
| 13 | La pieza Three.js no degrada el formulario en equipos lentos | RF-FE-BRF-06 | — | `U` | Riesgo aceptado: es decorativa, perezosa y se desactiva con `prefers-reduced-motion`. No compensa medir rendimiento | — | — | — |
| 14 | La segunda opinión se lee: una línea por conflicto, con su número, su veredicto y su motivo, enlazada a su conflicto; si falta, se dice | RF-FE-PAR-01 | Tests de pantalla con MSW: con la opinión, con un aviso antes de los conflictos (el número cuenta la lista entera), con formas fuera del esquema (nada se pierde) y con `segunda_opinion: null` | `T` | Qué dice la opinión, que es de un modelo: el cliente solo la pinta | Sí | `src/funcionalidades/ejecucion/AlertaParada.test.tsx` | implementado |
| 15 | La lectura muestra una versión publicada, con su portada, su índice y los capítulos cambiados marcados; sin versiones, lo dice | RF-FE-LEE-01 a RF-FE-LEE-03, RF-FE-LEE-06 | Tests de pantalla con MSW sobre dos versiones | `T` | Que el texto de la versión coincida con el vigente lo garantiza el backend (RF3-BIB-14), no el cliente | No: la fila 21 lo mira en el navegador | `src/funcionalidades/lectura/Lectura.test.tsx` | implementado |
| 16 | «Qué cambió» marca exactamente lo añadido y lo quitado | RF-FE-LEE-07 | Tests unitarios de la comparación: casos fijos, vacíos, repetidos y el tope, y dos propiedades: aplicar la comparación da las dos versiones exactas, y cambiar una palabra marca esa palabra y nada más | `T` | Cómo se ve: colores y tachados, en la fila 21 | No | `src/funcionalidades/lectura/comparar.test.ts`, `texto.test.ts` | implementado |
| 17 | Cada personaje y lugar de la ficha enlaza a los capítulos donde aparece según `/apariciones` (y, si no tiene, dónde se nombra en la prosa); en una versión anterior, con los nombres de entonces (de `/cambios`) y, en los capítulos que un relanzamiento reescribió después, buscando ese nombre en su prosa | RF-FE-LEE-04 | Tests de pantalla con MSW en la última versión, en una anterior y con un renombre deshecho; tests unitarios de `nombresEnVersion`, `aparicionesDeApi` y `reescritosDespues`; recorrido contra la API real con dos renombres (fila 19) | `T` + `D` | En los capítulos reescritos por un relanzamiento, la búsqueda por nombre no ve a quien está sin ser nombrado; el aviso lo dice. El relanzamiento no se probó contra el backend real | No | `src/funcionalidades/lectura/Lectura.test.tsx`, `apariciones.test.ts` | implementado |
| 18 | El cambio del lector manda el payload del contrato, solo desde la última versión, dice el alcance antes de confirmar y enseña rechazos y versión nueva | RF-FE-CAM-01 a RF-FE-CAM-05, sección 5.2 | Tests de integración con MSW que simula la intención y publica una versión | `T` | El contrato lo simula MSW: si el backend lo implementa distinto, solo lo ve la fila 19 | No: la fila 19 | `src/funcionalidades/lectura/CambioLector.test.tsx` | implementado |
| 19 | El cambio del lector funciona contra el backend real: pedir, ver reescribir y leer la versión nueva con sus cambios marcados | RF-FE-CAM, RF-FE-LEE-07 | Demostración con Playwright contra la API y el worker reales, primero con el puerto falso y después con Claude Code real (con aprobación del autor, por el coste) | `D` | Depende del bloque 8 del backend, que hace novelasv2-f4 | Sí | `src/frontend` ejecutado con Playwright contra la API (8001) y el worker reales con el puerto falso, sobre una base temporal (24-09): dos cambios seguidos (Vaan→Brann→Corvo) con su alcance, la franja, las versiones 2 y 3, las marcas del índice, el diff y la ficha de la versión 1 con «Vaan (ahora Corvo)»; y `npm run pdf` de las versiones 1 y 3. Destapó que la ficha de una versión anterior contradecía a la actual (entrada del registro de iteraciones) | implementado con el puerto falso; falta con Claude Code real |
| 20 | El PDF lleva portada, novedades, índice con enlaces internos, capítulos en página nueva y la ficha | RF-FE-PDF-01 a RF-FE-PDF-03 | Test de la vista de impresión con MSW (orden y anclas) + `npm run pdf` sobre una novela y lectura del PDF resultante | `T` + `I` | La maquetación fina de página (viudas, huérfanas) solo la ve una persona. Los enlaces del PDF se contaron en el fichero, no se pulsaron | No | `src/funcionalidades/lectura/Imprimir.test.tsx`. Revisión del PDF de la novela 6 de los mocks (24-09): 15 páginas, un capítulo por página, 77 enlaces internos y esquema de títulos. Corrigió dos fallos: el índice y el capítulo 1 compartían página con las novedades, y el fondo crema dejaba un marco en los márgenes | implementado |
| 21 | La lectura se ve bien: portada, índice, ficha, capítulos, novedades e impresión, inspeccionadas con el browser MCP | RF-FE-MCP-01, RF-FE-MCP-02 | Recorrido real de un agente con Playwright MCP, con capturas | `I` | Juicio del agente y del autor; no se repite solo | Sí | `docs/proceso/inspeccion-browser-mcp.md` | pendiente |
| 22 | La ficha usa `/apariciones` cuando la API la sirve (presencia real en la prosa), y la portada, la línea del regalo | RF-FE-LEE-02, RF-FE-LEE-04, sección 5.2 | Tests de pantalla con MSW con los campos nuevos, tras regenerar los tipos | `T` | Depende de que el backend publique los campos (novelasv2-f4) | Sí | `src/funcionalidades/lectura/Lectura.test.tsx`, `Imprimir.test.tsx` y `apariciones.test.ts`, con los tipos regenerados de `tests/openapi.json` | implementado |
| 23 | `/ejemplos/novela-ejemplo.pdf` sale de una novela real completada, con `npm run pdf` | RF-FE-PDF-03 | `npm run pdf` contra la API real y lectura del PDF | `D` + `I` | Que la novela sea buena lo juzgan las puertas y el autor, no el PDF | Sí | `/ejemplos/novela-ejemplo.pdf`, exportado el 24-09 con `npm run pdf` desde una copia de la novela de diez capítulos del brief de ejemplo (completada con avisos, Opus 5.5), servida por una API propia: 79 páginas A5, 81 enlaces internos, portada con dedicatoria y línea del regalo, índice, diez capítulos y el apéndice de personajes y lugares con sus capítulos. Leerlo destapó tres fallos que los mocks no enseñaban: la dedicatoria larga pisaba la foto de la portada impresa, las clases `ficha` de la consola se colaban en la ficha de la lectura, y 91 cursivas salían como asteriscos | implementado |
| 24 | La portada lleva la ilustración de su subgénero (la genérica sin él), en tamaño de pantalla en la web y de impresión en el PDF, y la vista de impresión espera a que esté cargada | RF-FE-IMG-01, RF-FE-IMG-04 | Tests unitarios de `portadaDe` para los seis subgéneros de `brief.ts` y el caso sin subgénero; tests de pantalla de la portada y de la vista de impresión con MSW, y de la espera con `decode` simulado (no se marca lista con la decodificación pendiente, y un fallo de carga no la bloquea) | `T` | jsdom no pinta ni decodifica: que el fondo se vea lo dice la fila 25 | No | `src/compartido/imagenes/imagenes.test.ts`, `Lectura.test.tsx`, `Imprimir.test.tsx` | implementado |
| 25 | Las imágenes se ven bien y no rompen la maquetación: la portada en escritorio, a 390 y a 360 px sin tapar el texto ni dar scroll horizontal; el plano sin marco sobre el papel; los dibujos en su color; la portada en el PDF | RF-FE-IMG-01 a RF-FE-IMG-03, RF-FE-VIS-04 | Capturas con Playwright contra los mocks y medida de desbordes (`scrollWidth` frente al ancho de la ventana) en las pantallas tocadas; primera página del PDF | `I` | Juicio de quien mira las capturas; no se repite solo | Sí | Capturas del 24-09. Corrigieron cuatro fallos: el texto de la portada pisaba la ilustración en móvil, la portada daba scroll horizontal a 390 px, el plano dejaba un marco más oscuro sobre el papel de la lectura y el PDF salía sin la ilustración. El validador encontró después rótulos legibles en dos portadas (slasher y terror corporal), que se borraron | implementado |
| 26 | Las novelas terminadas llevan la miniatura de su portada en su tarjeta y en su página, y las demás no; el alta enseña la portada del subgénero elegido | RF-FE-IMG-06, RF-FE-IMG-07 | Tests de pantalla con MSW del tablero (con `completada` y `completada_con_avisos`, y con resumen y ejecución que discrepan o sin ejecución: manda la ejecución), de la página de la novela (terminada y en curso) y del paso «El terror» (genérica y al elegir subgénero, con axe), más capturas a 1440, 1280 y 390 px sin scroll horizontal | `T` + `I` | Que la franja encuadre bien la foto lo dicen las capturas: el 78 % vertical está calculado para la maquetación de portada de hoy | No | `TableroGeneral.test.tsx`, `TableroNovela.test.tsx`, `CrearNovela.test.tsx` | implementado |
| 27 | La cursiva de la prosa (`*…*`) sale en cursiva y sin asteriscos en la lectura, el PDF, el lector de consola y la comparación, también cuando el cambio toca un asterisco; los asteriscos que no son marca se respetan | RF-FE-LEE-09 | Tests unitarios de `Prosa.tsx` con `compararPalabras` real (marca bien formada, `**`, `5 * 3 * 2`, cambio pegado al asterisco, renombre que abre y cierra cursivas, cursiva que cambia de extensión) y tests de pantalla de la lectura, la vista de impresión y la comparación; lectura del PDF de ejemplo | `T` + `I` | En la lectura de antes de una comparación, un trozo igual va con las marcas de después: si la cursiva cambió de extensión, un espacio puede salir en redonda | No | `src/compartido/ui/Prosa.test.tsx`, `Lectura.test.tsx`; `/ejemplos/novela-ejemplo.pdf`, capítulo 10 | implementado |
| 28 | Una parada `formal` (Lean) se puede resolver desde la web: ofrece solo `relanzar`, explica la consecuencia, nombra la comprobación y, con Lean no disponible, avisa de sus dos causas (sin instalar o sin terminar a tiempo) | RF-FE-PAR-02, RF-FE-PAR-04, RF-FE-PAR-05 | Tests de pantalla con MSW de una parada `formal` con el informe de `ResultadoPuerta` (puerta 6), con `lean_nadie_tras_morir` (relanza desde el capítulo de la parada) y con el `lean_no_disponible` del tiempo límite | `T` | `reglas.ts` copia la tabla de `RESOLUCIONES` a mano (fila 2): si el backend añade otra acción a `formal`, el frontend no la ofrece hasta actualizarla | No | `src/funcionalidades/ejecucion/AlertaParada.test.tsx` | implementado |
