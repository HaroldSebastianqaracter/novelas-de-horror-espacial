# Verificación — Backend v2

Plan de verificación de [spec2.md](spec2.md). Métodos y etiquetas según [docs/validators.md](../docs/validators.md). Las filas de [spec1-verification.md](spec1-verification.md) siguen vigentes; aquí solo están las propiedades que spec2 añade o endurece.

Actualizado el 23 de septiembre de 2026. Las rutas de evidencia son relativas a `src/backend/`.

## Cómo leer la tabla

**Punto ciego** es lo que ese método no ve aunque pase (regla 1 de validators.md). **¿Solitario?** dice si la propiedad descansa en un solo método; si lo hace, se revisa antes que ninguna otra aunque esté en verde (regla 2). **Estado** es `implementado`, `pendiente` o `fallando`. Una fila cuya evidencia es un test marcado `xfail` está `pendiente`: el test existe y falla, que es justo lo que tiene que hacer hasta que la fase lo arregle.

## Propiedades verificadas

| # | Propiedad | Fase | Método | Tag | Punto ciego | ¿Solitario? | Evidencia | Estado |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | Ninguna salida del bucle de capítulo deja estado del capítulo sin cerrar | 1 | Integration testing, una prueba por tipo de salida | `T` | Solo las salidas que el test enumera; una excepción nueva en otro sitio no está cubierta | No: la regla de integridad lo ve después | `tests/test_capitulo_a_medias.py` (salida inválida del juez, agente interrumpido, reanudación), `tests/test_auditoria.py::test_hallazgo_05_…` (`parar`) | implementado |
| 2 | El worker caído deja el grafo en el último capítulo íntegro | 1 | Integration testing con una caída real del proceso | `T` | Una caída a mitad de una transacción la cubre SQLite, no este test; solo prueba la caída durante el oficio | No: la regla `estado_en_capitulo_no_completado` lo ve también | `tests/test_auditoria.py::test_hallazgo_06_…` | implementado |
| 3 | Ningún capítulo se genera sin las puertas 1 y 2 vigentes | 2 | Model checking de estados + `avanzar`: recorrido por anchura de los estados alcanzables con cada suceso y cada combinación de puertas, más una mutación que comprueba que el recorrido detecta el `avanzar` de spec1 | `A` | Solo ve el orden, con puertas falsas; se fía de que `resultado_puerta` sea veraz (la puerta 4 lo arregla la fase 6). Solo las caídas que enumera | No: guardarraíl de la fila 4 | `tests/test_estados_exhaustivo.py`, `tests/test_auditoria.py::test_hallazgo_01_…`, `tests/test_reanudacion.py::test_una_caida_entre_la_estructura_y_la_puerta_1_no_salta_la_puerta` | implementado |
| 4 | Lo mismo, en ejecución | 2 | Guardarraíl en `generar_capitulo` | `D` | El mismo: la vigencia se lee de `resultado_puerta` | No: model checking de la fila 3 | `orquestador/pipeline.py::generar_capitulo` (`PuertasNoVigentes`); `tests/test_reanudacion.py::test_generar_un_capitulo_sin_puertas_vigentes_acaba_en_error` | implementado |
| 5 | Resolver una parada rehace lo que la puerta rechazó | 2 | Integration testing | `T` | Comprueba que se rehace, no que la segunda versión sea mejor | **Sí** | `tests/test_reanudacion.py` (rehacer escaleta y estructura, tabla de acciones, retcon), `tests/test_auditoria.py::test_hallazgo_02_…`, `::test_hallazgo_12_…` | implementado |
| 6 | Dos workers no escriben a la vez | 3 | Integration testing con dos procesos reales + robo del cerrojo a mitad del pipeline | `T` | Un proceso congelado más tiempo que la gracia puede terminar la transacción que ya tenía abierta; el fencing lo acota a esa transacción. Una base de datos en red, donde el cerrojo de SQLite no es fiable | No: fencing + latido, con un test cada uno | `tests/test_cerrojo.py`, `tests/test_auditoria.py::test_hallazgo_03_…`; los tests del fencing fallan si la guarda se anula | implementado |
| 7 | Ningún elemento obligatorio se pierde; ningún recorte es silencioso | 4 | Property-based testing | `T` | Lo marcado opcional puede ser lo que el capítulo necesitaba: la relevancia es una heurística. La estimación de tokens es la de spec1 (U2-1) | **Sí** | `tests/test_paquete.py::test_lo_obligatorio_nunca_se_pierde_y_ningun_recorte_es_silencioso`, `::test_un_techo_bajo_llega_al_paquete_y_el_recorte_queda_en_la_traza`; `tests/test_auditoria.py::test_hallazgo_04_…`, `::test_hallazgo_10_…` | implementado |
| 8 | Una cadena de supersesiones no produce contradicciones | 5 | Mutation testing | `T` | Supersesiones que el extractor no declaró siguen siendo contradicción: correcto, pero es falso positivo si el extractor olvida marcar | No: la cadena y el par único tienen test cada uno | `tests/test_puerta_continuidad.py::test_una_cadena_de_tres_supersesiones_no_es_contradiccion`, `::test_cada_par_contradictorio_se_informa_una_vez`; `tests/test_auditoria.py::test_hallazgo_07_…` | implementado |
| 9 | Dos valores que solo difieren en mayúsculas o tildes son el mismo | 5 | Unit testing | `T` | Sinónimos («castaño» y «marrón»): sigue siendo U7 de spec1. La ene se conserva a propósito | — | `tests/test_puerta_continuidad.py::test_mayusculas_tildes_y_espacios_no_cambian_el_valor`, `::test_la_ene_es_una_letra_y_si_distingue`; `tests/test_auditoria.py::test_hallazgo_08_…` | implementado |
| 10 | `condicion = muerto` impide reaparecer | 5 | Mutation testing | `T` | **Dato autodeclarado por el extractor** (regla 3): una muerte que no registró como `muerto` no para | No, con la fila 13 (fase 6) | `tests/test_puerta_continuidad.py::test_detecta_personaje_muerto_que_reaparece`, `::test_la_ultima_condicion_manda`, `::test_la_muerte_no_impide_una_analepsis`; `tests/test_auditoria.py::test_hallazgo_16_…` | implementado |
| 11 | `resultado_puerta` refleja las dos partes de la puerta 4 | 6 | Contract testing | `T` | No dice si el juez acierta (fila 39 de spec1) | No: el contrato y el caso de mecánica fallida tienen test cada uno | `tests/test_traza.py::test_toda_parada_de_oficio_tiene_la_puerta_4_en_falla`, `::test_un_juicio_en_contra_queda_registrado_con_su_evidencia`, `::test_si_la_mecanica_falla_el_registro_dice_que_no_hubo_juicio`; `tests/test_auditoria.py::test_hallazgo_09_…` | implementado |
| 12 | El extractor no descarta en silencio | 6 | Integration testing sobre el recuento | `T` | Lo que el extractor ni siquiera emitió | No, con la fila 13 | `tests/test_traza.py::test_los_descartes_del_extractor_van_a_la_traza_por_motivo`, `tests/test_auditoria.py::test_hallazgo_11_…` | implementado |
| 13 | Cobertura del extractor desde el texto | 6 | Búsquedas dirigidas (nombres, cifras, muertos nombrados) | `T` | Rasgos sin nombre propio ni cifra; alias y pronombres; un personaje nombrado sin nada que extraer da un aviso de más | No: complementa las evals del extractor (`I`, fila 38) y la fila 10 | `tests/test_traza.py` (sección RF2-PIPE-17) | implementado |
| 14 | El agente no usa herramientas | 6 | Guardarraíl en el puerto | `D` | Depende de que el CLI siga devolviendo `permission_denials`; `num_turns` dejó de ser señal tras dar 3 en una llamada real legítima. Lo retirado por `--tools ""` sí se probó contra el CLI real (primera pasada del pipeline, 23-09-2026) | **Sí** | `compartido/puerto/terminal.py::_uso_de_herramientas`; `tests/test_traza.py` (sección RF2-PUERTO-10), `tests/test_auditoria.py::test_hallazgo_17_…` | implementado |
| 15 | El filtro admite antes de que la similitud ordene | 7 | Unit testing | `T` | Solo el filtro por lugar y capítulo; la amenaza compartida que pedía RF-CTX-07 no entra en el filtro | No: el golden set de la fila 16 lo ejerce también | `tests/test_indice.py::test_el_filtro_admite_antes_de_que_la_similitud_ordene` (50 escenas de otro lugar más parecidas a la consulta) | implementado |
| 16 | Lo recuperado es relevante | 7 | Golden set, recall@k, sin juez | `T` | Golden set pequeño (8 consultas), escrito por el equipo y con vocabulario compartido con las escenas: recall@3 = 1,0 con `potion-multilingual-128M` es optimista. U8 sigue abierto | **Sí** | `tests/test_indice.py::test_golden_set_de_recuperacion_con_el_modelo_real` (marcado `modelo`; se corre con `pytest -m modelo`) | implementado |
| 17 | Toda respuesta de la API tiene esquema, y el contrato no cambia sin revisión | 8 | Contract testing + snapshot | `T` | Forma, no semántica: un campo bien tipado puede traer un valor sin sentido. Los campos libres a propósito (`informe`, `metadatos`, `resultado`) siguen siendo objetos abiertos | No: esquema + snapshot | `tests/test_api.py::test_ninguna_respuesta_es_un_objeto_sin_esquema`, `::test_el_contrato_no_cambia_sin_revisarlo`, `::test_cada_entidad_del_canon_sale_con_su_esquema`, y los 422 de payloads mal formados | implementado |
| 18 | Tipos consistentes | 8 | Type checking (pyright strict) | `A` | No ve el contenido real de las filas de SQLite: `como_dict` y `como_lista` afirman una forma que la base no garantiza. Los tests quedan fuera | No | `pyright` en `pyproject.toml` (`venvPath`, `venv`, estricto): 0 errores | implementado |
| 19 | La demo ejercita todas las comprobaciones de la puerta 3 | 10 | Integration testing con cobertura por comprobación + mutation testing sobre el recorrido entero | `T` | Los datos los fabrica el propio equipo: prueba el mecanismo, no el extractor real (fila 32). Las mutaciones se hacen en el capítulo 2; `sorpresa_imposible` es aviso y solo se comprueba que salga, no que pare | No: cobertura + mutaciones | `tests/test_demo.py` (una consulta de datos por comprobación, la sorpresa como aviso y siete mutaciones que paran el pipeline cada una con su comprobación) | implementado |
| 20 | Pipeline completo con Claude Code real | 10 | Demonstration | `D` | Una novela corta no enseña lo que pasa hacia el capítulo 30. Una sola pasada, con un solo modelo | **Sí** | Pasada del 23-09-2026 sobre `novela_real.db`: planificación, puertas 1 y 2 y capítulos 1 y 2 completos; parada por el autor en el capítulo 3 con tres huecos de diseño abiertos (U2-4 a U2-6). 26 llamadas, 11,57 $. Detalle en spec2.md, «Resultado de la pasada real» | pendiente |
| 21 | Un hecho no se modifica; revocarlo es un registro y relanzar desde antes lo deshace | 5 | Integration testing sobre los triggers y la reversión | `T` | Un `UPDATE` sobre las columnas muertas `parada_id` y `supersede_a` sigue permitido para que sus cascadas actúen | No: trigger + test de reversión | `tests/test_migraciones.py`, `tests/test_reanudacion.py::test_el_retcon_es_un_registro_y_relanzar_desde_antes_lo_deshace`, `tests/test_auditoria.py::test_hallazgo_21_…` | implementado |
| 22 | Dos entidades de una novela no comparten nombre normalizado | 5 | Integration testing (índice único) + unit testing de los esquemas | `T` | Nombres distintos para la misma entidad («la doctora» y «Kowalski») | No: índice + esquema | `tests/test_auditoria.py::test_hallazgo_20_…`, `tests/test_migraciones.py::test_con_nombres_duplicados_la_migracion_aborta_con_la_lista_y_no_toca_nada` | implementado |
| 23 | Una base del esquema 1 migra a la 2 sin perder datos, y con nombres duplicados aborta sin tocar nada | 5 | Integration testing | `T` | Solo los datos que el test fabrica; una base real con formas no previstas | **Sí** | `tests/test_migraciones.py` | implementado |
| 24 | Una señal de terminar para el pipeline aunque llegue entre dos llamadas | 6 | Integration testing | `T` | Solo prueba la señal antes de la primera llamada y en el puerto; no envía una señal real al proceso | **Sí** | `tests/test_traza.py::test_una_senal_entre_llamadas_para_sin_llamar_a_ningun_agente`, `::test_parar_por_intencion_solo_corta_una_llamada`; `tests/test_auditoria.py::test_hallazgo_18_…` | implementado |
| 25 | El índice sabe con qué modelo se construyó y se rehace si cambia; sin modelo no hay hash de repuesto | 7 | Integration testing | `T` | Mismo nombre de modelo con otra versión de pesos: no se detecta | No: registro + test de reconstrucción | `tests/test_indice.py::test_cambiar_de_modelo_rehace_el_indice_con_la_dimension_nueva`, `::test_sin_modelo_no_hay_hash_de_repuesto` | implementado |
| 26 | Todo fallo del índice es un aviso en la traza, y toda reversión lo purga | 7 | Integration testing | `T` | Solo los fallos de carga del modelo; un fallo a mitad de una consulta sigue el mismo camino pero no tiene test propio | No | `tests/test_indice.py::test_si_el_modelo_no_carga_el_pipeline_sigue_y_la_traza_lo_avisa`, `::test_revertir_saca_del_indice_lo_que_deja_de_ser_vigente` | implementado |
| 27 | El orden global de escena no colisiona | 9 | Unit testing + trigger | `T` | Un capítulo con más de 999.999 escenas se rechaza en vez de ordenarse: es un límite, no un orden | No: trigger + test | `tests/test_deuda.py::test_el_ordinal_de_escena_no_colisiona_con_capitulos_largos`, migración 003 | implementado |
| 28 | La puerta 5 avisa del pago sin siembra, del cierre fuera de orden y de la latencia por tramo continuo, sin bloquear | 9 | Unit testing | `T` | Comprueba que el pago y el cierre **existen** en orden, no que satisfagan | No | `tests/test_deuda.py` (sección puerta 5) | implementado |
| 29 | `insertar` y `actualizar` solo escriben columnas que existen | 9 | Unit testing | `T` | Valida nombres de columna, no el tipo del valor | No | `tests/test_deuda.py::test_insertar_una_columna_desconocida_es_un_error_antes_de_llegar_a_sqlite`, `::test_actualizar_puede_poner_una_columna_a_null` | implementado |
| 30 | Los hilos cambian de estado según lo que declara el extractor, y un hilo que no estaba en la lista se descarta y cuenta | 10 | Integration testing | `T` | Que el extractor real declare los estados que la prosa hace; eso lo mide la fila 32 | No: el recorrido de la demo + el descarte | `tests/test_demo.py::test_la_siembra_y_los_hilos_recorren_su_ciclo`, `::test_un_hilo_que_no_esta_en_la_lista_se_descarta_y_cuenta` | implementado |
| 31 | Repetir un hecho vigente no crea otra fila | 10 | Integration testing | `T` | Un valor que repite con otras palabras («castaño» frente a «pelo castaño») sí crea fila: compara claves, no significado | No: el test + la demo sin falsos `conocimiento_no_adquirido` | `tests/test_demo.py::test_repetir_un_rasgo_no_crea_otro_hecho`, `::test_la_demo_termina_limpia` | implementado |
| 32 | El extractor real captura lo que un capítulo anotado fija | 10 | Evals con golden dataset, recall por tipo de registro, sin juez | `I` | Un solo capítulo de dos escenas, anotado por el equipo; las claves casan por subcadena y un registro con la clave pero el sentido equivocado cuenta como acierto. No mide precisión (lo que el extractor inventa) | **Sí** | `tests/test_evals_extraccion.py`: el dataset anclado en su prosa y el puntuador, probados con salidas sintéticas; la pasada real (`pytest -m agente`, umbrales 0,8 total y 0,75 en hechos) ejecutada el 23-09-2026 con Claude Code 2.1.274: recall 15/15 en todos los tipos. Una sola pasada sobre un capítulo corto y explícito: es optimista, como el golden set de la fila 16 | implementado |
| 33 | Un resumen del extractor más largo que su límite se recorta y queda en la traza, sin invalidar la extracción | 10 | Unit testing + integration testing | `T` | Cuenta palabras separadas por espacios; una frase sin puntuación final se corta por la palabra | No: el recorte + el evento de traza | `tests/test_deuda.py::test_un_resumen_largo_se_recorta_por_la_ultima_frase_completa`, `::test_una_extraccion_con_el_resumen_largo_entra_recortada_y_la_traza_lo_dice` | implementado |
| 34 | La puerta 3 da por sabido lo que el personaje presenció, sigue parando lo que usa sin haber estado, y una entidad inventada es aviso | 10 | Unit testing + mutation testing sobre la demo | `T` | Estar en el reparto no es enterarse: un personaje presente y ajeno lo «sabe». Lo inventado no entra en el canon y nadie comprueba que se mantenga | No: unidad + mutación | `tests/test_puerta_continuidad.py::test_estar_en_la_escena_donde_se_fija_el_hecho_es_saberlo`, `::test_detecta_conocimiento_no_adquirido`, `::test_detecta_entidad_fuera_de_canon`; `tests/test_demo.py::test_romper_la_demo_en_una_comprobacion_para_el_pipeline[_usa_sin_saber-…]`, `::test_una_entidad_inventada_es_un_aviso_y_la_demo_sigue` | implementado |
| 35 | El extractor ve el valor vigente de cada atributo, y un hecho cuya cita solo está en otra escena se registra en esa escena | 10 | Integration testing | `T` | Que el modelo repita el valor exacto es una instrucción, no una garantía; una cita reformulada no se puede localizar y deja la escena declarada | No: paquete + corrección por cita | `tests/test_demo.py::test_el_paquete_del_extractor_trae_el_valor_vigente`, `::test_la_cita_manda_sobre_el_numero_de_escena` | implementado |
| 36 | Un uso o un conocimiento apunta al hecho vigente en su escena, y los hechos de una salida se aplican en orden de escena | 10 | Integration testing, rojo comprobado contra el código anterior | `T` | Un uso anterior al primer hecho de su sujeto y atributo queda sin resolver y sale como aviso `conocimiento_sin_comprobar`, no como conflicto | No: el test + la puerta 3 | `tests/test_demo.py::test_un_uso_apunta_al_hecho_vigente_en_su_escena_no_al_ultimo_del_capitulo` | implementado |
| 37 | Una parada de conocimiento se resuelve dando por sabido lo contado fuera de escena, y sin conflicto de conocimiento la acción se rechaza | 10 | Integration testing | `T` | La confirma una persona: nada comprueba que el personaje pudiera enterarse de verdad. Un hecho del mismo capítulo rechazado no se puede dar por sabido | No: el test + la tabla de estados | `tests/test_reanudacion.py::test_dar_por_sabido_registra_lo_contado_y_el_capitulo_pasa`, `::test_dar_por_sabido_sin_conflicto_de_conocimiento_se_rechaza`, `tests/test_estados_exhaustivo.py` | implementado |
| 38 | Un lugar y los que contiene son el mismo sitio para el objeto sin traslado y la presencia imposible; el mundo rechaza contenedores desconocidos y ciclos | 10 | Unit testing sobre la puerta, con el mismo grafo detectado sin la contención (la presencia imposible para; el objeto sin traslado avisa desde RF2-PIPE-32) | `T` | La jerarquía la declara el constructor de mundo: un lugar sin contenedor declarado sigue siendo un sitio aparte. Un traslado dentro del mismo contenedor ya no se ve | No: puerta + esquema | `tests/test_puerta_continuidad.py::test_un_objeto_que_pasa_a_un_lugar_contenido_no_se_ha_movido`, `::test_estar_a_la_vez_en_un_lugar_y_en_otro_que_contiene_no_es_ubicuidad`, `tests/test_deuda.py::test_el_mundo_rechaza_contenedores_desconocidos_y_ciclos` | implementado |
| 39 | El extractor real cubre lo que la prosa real afirma y no reformula valores vigentes | 10 | Evals con golden dataset sobre la prosa de la pasada real, recall por tipo más contradicciones falsas, sin juez | `I` | Anotación del equipo, solo de lo evidente; paquete congelado, así que la pasada `agente` no mide cambios del paquete. Dos capítulos | No: recall + contradicciones falsas, y la fila 32 | `tests/test_evals_extraccion.py` (dataset anclado en su prosa y su canon, puntuador por prefijo y emparejamiento máximo, línea base 32/33 y 30/32 sin gastar, contradicciones falsas 6→0 en los intentos del capítulo 2); la pasada real (`pytest -m agente`) sin ejecutar | implementado |
| 40 | Entre capítulos, un personaje sabe lo que su facción sabía; un objeto viaja con su poseedor | 10 | Unit testing sobre la puerta, con el mismo grafo detectado sin la regla (el conocimiento para; el objeto sin traslado avisa desde RF2-PIPE-32); comprobado antes contra los conflictos reales de la pasada | `T` | La facción la declara el elenco: un personaje mal encuadrado recibe lo que no debería. Un objeto que el poseedor deja sin registrarlo sigue «viajando» con él | No: dos reglas con su test cada una, y los casos reales | `tests/test_puerta_continuidad.py::test_lo_que_sabia_la_faccion_en_un_capitulo_anterior_lo_sabe_el_miembro`, `::test_dentro_del_mismo_capitulo_la_faccion_no_transmite`, `::test_un_objeto_que_lleva_alguien_viaja_con_el` | implementado |
| 41 | Un cambio de valor en un atributo que es conducta es aviso; la marca es estado con escena de origen | 10 | Unit testing sobre la puerta + integration testing sobre la demo | `T` | La marca la pone el extractor (autodeclarada): un rasgo marcado por error como conducta deja de parar. Una marca en cualquier escena anterior basta | No: puerta + demo | `tests/test_puerta_continuidad.py::test_cambiar_el_valor_de_una_conducta_es_aviso`, `tests/test_demo.py::test_un_habito_roto_a_proposito_avisa_y_la_demo_sigue`, `tests/test_arquitectura.py::test_el_estado_es_append_only_con_escena_de_origen` | implementado |
| 42 | Quien actúa en una escena (uso de conocimiento o estado registrado allí) está en ella para la puerta 3, aunque la escaleta no lo pusiera | 10 | Unit testing sobre la puerta, con el mismo grafo parando sin el registro | `T` | Alguien presente a quien el extractor no registra nada sigue contando como ausente | No | `tests/test_puerta_continuidad.py::test_quien_actua_en_la_escena_esta_en_ella_aunque_no_estuviera_en_el_reparto` | implementado |
| 43 | La conexión de una petición de la API se puede abrir, usar y cerrar en hilos distintos | 8 | Unit testing sobre las dos dependencias, abriendo en un hilo y usando y cerrando en otro; rojo comprobado antes del arreglo | `T` | No prueba carga concurrente real contra el servidor, solo el mecanismo que la rompía | No | `tests/test_api.py::test_la_conexion_de_una_peticion_se_puede_cerrar_desde_otro_hilo` | implementado |
| 44 | Quien el extractor registra presente en una escena está en ella para la puerta 3 (conocimiento propio, facción, deducción y poseedor), aunque la escaleta no lo pusiera; la presencia se revierte con su escena | 10 | Unit testing sobre la puerta, con el mismo grafo parando (o avisando, en la deducción) sin la presencia; unit testing de la reversión por escena de origen | `T` | La presencia es autodeclarada: un extractor que marque de más silencia `conocimiento_no_adquirido`; el muerto y la ubicuidad no la leen | No: la fila 42 y la de RF3-PAS-09 en spec3 | `tests/test_puerta_continuidad.py::test_quien_el_extractor_registra_presente_esta_en_la_escena`, `::test_la_presencia_extraida_cuenta_para_la_faccion_y_el_poseedor`, `::test_la_presencia_extraida_cuenta_para_la_deduccion`, `::test_revertir_un_capitulo_borra_sus_presencias`; el extractor que la escribe, en la fila 38d de spec3 | implementado |
| 45 | El objeto sin traslado avisa y no para; el mismo sitio y el poseedor siguen callándolo | RF2-PIPE-32 | Unit testing sobre la puerta (detectado pero no bloqueante) e integration testing con la demo, que se completa con el aviso en el informe | `T` | Un objeto que la prosa mueve sin que el extractor lo registre ya no para: queda como aviso (caso C05 del banco) | No: el banco de la puerta 3 fija lo mismo | `tests/test_puerta_continuidad.py::test_detecta_objeto_sin_traslado`, `tests/test_demo.py::test_un_objeto_sin_traslado_es_un_aviso_y_la_demo_sigue`, `tests/test_banco_puerta3.py` (C05) | implementado |

## Riesgos aceptados

| # | Propiedad | Por qué no se verifica | Qué lo hace tolerable |
| --- | --- | --- | --- |
| U2-1 | La estimación de tokens no se desvía del recuento real | Sigue sin tokenizador de referencia | Es U4 de spec1, con su mismo atenuante |
| U2-2 | El desempate por similitud de RF-CTX-08 mejora algo | No hay golden set que lo mida | El orden determinista con desempate por `id` es reproducible; se revisa cuando exista el golden set de la fase 7 |
| U2-3 | La estructura entera cabe en una sola llamada al escaletador | La escaleta por actos de RF-PIPE-06 no se implementa: el escaletador recibe texto, no un paquete con presupuesto | Las novelas de prueba son cortas; si la estructura no cabe, la llamada falla y la ejecución acaba en `error`, visible, no en una escaleta truncada |
| U2-4 | Un hábito que se rompe no es una contradicción | Resuelto con la marca de conducta (RF2-PIPE-29, fila 41), que pone el extractor: si no la pone, el hábito roto sigue parando | Para la generación, que es lo seguro; el catálogo de mutabilidad (idea guardada) lo haría determinista |
| U2-5 | Lo que sabe un grupo lo saben sus miembros | Resuelto por facción entre capítulos (RF2-PIPE-27, fila 40). Queda el grupo que no es facción (dos personajes que comparten turno sin compartir bando) | `dar_por_sabido` cubre el resto, caso a caso |
| U2-6 | Un objeto de información (un documento, una orden) puede consultarse en varios sitios | El caso de la pasada era un objeto físico con poseedor, resuelto por RF2-PIPE-28 (fila 40). Un objeto de información de verdad sigue teniendo una sola ubicación | Como aviso de objeto sin traslado en el informe de la puerta 3 (RF2-PIPE-32, fila 45): visible, pero ya no para |

## Validadores solitarios

Las filas 5, 7, 14, 16, 20, 23, 24 y 32 descansan en un solo método. Son las primeras que se revisan al cerrar cada fase, aunque estén en verde.
