[pytest]
pythonpath = .
markers =
    modelo: necesita un modelo de embeddings real descargado en la cache local
    agente: llama a Claude Code de verdad; cuesta dinero y exige la aprobacion del autor
addopts = -m "not modelo and not agente"
