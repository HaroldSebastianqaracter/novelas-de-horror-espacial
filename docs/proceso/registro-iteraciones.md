# Registro de iteraciones

Qué cambió tras cada eval, auditoría, test o ejecución real, y por qué. No es un diario: cada entrada es una decisión con su causa y su efecto, y apunta a la evidencia (commit, test o requisito) para poder comprobarla.

**Cómo se añade una entrada.** Una fila por cambio provocado por algo observado. *Disparador* es lo que lo destapó (auditoría, test, eval, ejecución real, contraejemplo de TLC o Lean). *Evidencia* es el commit o el test donde se ve. Lo que se decidió sin nada que lo disparase va en [trade-offs](trade-offs.md), no aquí.

## Septiembre de 2026

| # | Fecha | Disparador | Qué se vio | Cambio | Efecto y evidencia |
| --- | --- | --- | --- | --- | --- |
| 0 | 21-09 | Petición de la profesora | Había que construir un proyecto nuevo sin borrar el anterior. El primer harness (15 al 20 de septiembre) orquestaba desde Claude Code con subagentes y hooks | Rama `novelasv2` desde cero, con el primero conservado en `main`: dominio, verificación y arquitectura antes que código, y el orquestador como código propio | Commit `764fe04` y los documentos de `docs/` |
| 1 | 22-09 | Revisión del catálogo de verificación | `validators.md` decía lo que cada método **detecta** y nunca lo que se le **escapa**: una tabla podía quedar entera en verde sostenida por un solo validador ciego | Columna de punto ciego obligatoria, tres reglas (punto ciego, validador solitario, dato autodeclarado) y principio de picaresca | Commit `b5fa475`; validators.md, «Puntos ciegos y validadores solitarios» |
| 2 | 23-09 | Auditoría del backend | La «prueba pequeña» que pasó en verde se hizo con el puerto falso y con un solo atributo: la puerta 3 no tenía nada que comparar. La auditoría reprodujo diez fallos que la suite de 53 tests no veía | Plan de corrección en diez fases (spec2), empezando por degradar a `fallando` las siete filas del plan de verificación que los fallos contradecían | [spec2-plan.md](../../specs/spec2-plan.md); commit `03d588f` con once reproducciones como `xfail` estricto |
| 3 | 23-09 | Reproducción E6 (auditoría) | Un worker caído entre el tramo 2 y el 3 dejaba el texto y los hechos de un capítulo sin terminar, y la comprobación de integridad no lo veía | Toda salida del bucle que no cierre el capítulo lo revierte; `recuperar()` revierte al arrancar; regla nueva de integridad | Commit `55c3bab`; `tests/test_capitulo_a_medias.py`; RF2-PIPE-08, RF2-FALLO-06 |
| 4 | 23-09 | Reproducción E3 (auditoría) | Resolver una parada de estructura dejaba la ejecución en `generando` sin capítulos y la terminaba como `completada`: las puertas 1 y 2 se saltaban | Las paradas 1 y 2 rehacen su fase (decisión entrevistada); `avanzar` deriva del grafo qué toca; las puertas valen por la huella de lo que juzgaron | Commit `68d9e7e`; `tests/test_estados_exhaustivo.py` detecta el `avanzar` anterior por mutación |
| 5 | 23-09 | Reproducción E5 (auditoría) | El cerrojo del escritor único caducaba a los seis segundos y nadie latía durante un capítulo: un segundo worker podía escribir a la vez | Latido en un hilo propio y fencing dentro de cada `BEGIN IMMEDIATE` | Commit `8649c70`; `tests/test_cerrojo.py` con dos procesos reales |
| 6 | 23-09 | Reproducción del recorte (auditoría) | Con unas 700 filas de conocimiento, el bloque de hechos del paquete desaparecía entero sin avisar: estaba unido en un solo párrafo y el recorte cortaba por párrafos | El paquete pasa a listas de elementos obligatorios u opcionales; si lo obligatorio no cabe, parada | Commit `161e150`; propiedad de `hypothesis` en `tests/test_paquete.py` |
| 7 | 23-09 | Reproducciones E1 y E2 (auditoría) | La puerta 3 daba contradicciones falsas cuando un atributo cambiaba dos veces seguidas, y cuando dos valores solo diferían en una tilde (`LOWER()` de SQLite solo pliega ASCII) | Claves normalizadas en Python y supersesión transitiva; la muerte, por un campo cerrado (`condicion`) y no por «muert» en texto libre | Commit `fabe8ae`; RF2-PIPE-11, RF2-PIPE-12 |
| 8 | 23-09 | Test de concurrencia de la fase 5 | Varios procesos abriendo a la vez una base nueva: uno recibía `database is locked` al ponerla en WAL | Poner la base en WAL se reintenta | Commit `fabe8ae` |
| 9 | 23-09 | Reproducción E7 (auditoría) | La puerta 4 registraba `pasa` tres veces seguidas mientras el juez fallaba el capítulo y se abría una parada: la traza mentía | La puerta 4 se registra entera, con un conflicto por criterio fallado | Commit `89fe379`; RF2-PIPE-13 |
| 10 | 23-09 | Auditoría (lectura del índice) | El índice vectorial buscaba los 40 vecinos más cercanos de todo el índice y filtraba después; las escenas buenas quedaban tapadas | La distancia se calcula solo sobre los candidatos que admite el filtro | Commit `ea39db4`; test rojo previo con 50 escenas de otro lugar; golden set: recall@3 = 1,0 en 8 consultas |
| 11 | 23-09 | Test de cobertura de la demo (fase 10) | La demo no generaba datos para varias comprobaciones de la puerta 3, y el test lo destapó: nadie escribía `hilo_estado`, y repetir un hecho vigente creaba una fila nueva que producía un falso «conocimiento no adquirido» | El extractor registra el estado de los hilos; un hecho repetido no crea fila | Commit `fa89af3`; `tests/test_demo.py`; RF2-PIPE-18, RF2-PIPE-19 |
| 12 | 23-09 | Eval del extractor con Claude Code real | Primera medición real de la pieza frágil del diseño | Ninguno: recall 15 de 15 contra el capítulo anotado | Commit `a3819c7`; fila 38 de spec1-verification |
| 13 | 23-09 | Primera llamada real del pipeline (arquitecto) | La llamada devolvió `num_turns = 3`, sin permisos denegados y con salida válida, y el guardarraíl la rechazó por sospecha de uso de herramientas | El número de turnos deja de contar como señal; solo cuentan los permisos denegados | Commit `59db690`; RF2-PUERTO-10 |
| 14 | 23-09 | Test de la puerta 3 con el brief (bloque 2) | Una novela personalizada de dos capítulos nunca pasaba la puerta 2: la estructura tiene tres actos y el tercero se quedaba sin capítulos. El fallo no era del test sino del producto: el brief admitía de 1 a 10 capítulos | El brief exige al menos tres capítulos | Rama `bloque2-personalizacion`; `tests/test_brief.py::test_una_novela_tiene_al_menos_tres_capitulos`; spec3, RF3-BRF-01 |
| 15 | 23-09 | Revisión del subagente `validador-de-codigo` sobre el bloque 2 | Veredicto RECHAZADO con un bloqueante y seis importantes, todos reproducidos: el filtro de citas se burlaba con una cita de una letra o con un valor inventado junto a una cita real; una orden en el texto libre entraba como recuerdo obligatorio; cambiar la huella dejaba sin vigencia todas las novelas antiguas, y ningún test lo cubría; una dedicatoria mal escrita dejaba una parada sin salida; la entrevista no podía quitar un vetado | Valor anclado en la cita; texto libre como material opcional y descarte de lo que dispara un patrón; lecturas del encargo en la huella solo con brief; rehacer desde la fase culpable; operación «quitar» en las listas | Rama `bloque2-personalizacion`; 13 tests nuevos, uno por escenario del validador; RF3-ENT-02, RF3-ENT-05, RF3-PER-07 |
| 16 | 23-09 | Lectura del extractor al diseñar el bloque 3 | Cuando el extractor repetía un hecho vigente, el código lo descartaba sin rastro (RF2-PIPE-19). Además de perder dónde se usa el hecho, dejaba un falso aviso `nombre_sin_registro`: la escena nombraba a un personaje, el extractor sí había dicho algo de él y la puerta 3 no lo veía | La reafirmación sigue sin crear un hecho, pero deja un uso con su cita (`hecho_uso`), y la búsqueda dirigida lo cuenta como registro. Lo mismo para `cifra_sin_hecho` con una cifra que ya está en el canon | Rama `bloque3-story-bible`; `tests/test_story_bible.py::test_una_reafirmacion_cuenta_como_registro_de_la_entidad_nombrada`; spec3, RF3-BIB-01 y RF3-BIB-03 |
| 17 | 23-09 | Revisión del subagente `validador-de-codigo` sobre el bloque 3 | Veredicto RECHAZADO, con los hallazgos reproducidos. El bloqueante: añadir la edad a la huella cambiaba la de toda novela con brief anterior al bloque, que al migrar perdía la vigencia de su puerta 1; una ejecución en `generando` se atascaba y, al reevaluarse, la puerta rechazaba siempre por una edad nula. Además: `dia_contra_orden` hacía chocar una analepsis con los antecedentes del mundo, cuyo orden el extractor no ve; dejaba escapar dos simultáneos en días distintos; `version_desfasada` no veía un título cambiado ni un capítulo que ya no existe; se buscaban menciones de valores ya sustituidos | La edad entra en la huella solo con brief y edades, y la comprobación no se aplica sin edad; `dia_contra_orden` compara solo eventos dramatizados fuera de una analepsis y detecta simultáneos; la regla de integridad compara la versión entera; los valores sustituidos no se buscan | Rama `bloque3-story-bible`; 5 tests nuevos, uno por hallazgo, más el ajuste del de la analepsis; spec3, RF3-BIB-06, RF3-BIB-09 y RF3-BIB-14 |

## Lecciones del primer harness que siguen vigentes

El historial anterior al reinicio deja una medición que aplica al bloque de Langfuse del plan de entrega: **Langfuse declaraba menos de la cuarta parte del coste real**, porque solo anotaba el consumo de los subagentes y no el de la sesión que orquestaba (commit `2280640`, 18 de septiembre). En el diseño actual el orquestador es código y no consume tokens, pero la lección se mantiene: el coste de cada llamada se tiene que registrar desde el puerto, no reconstruirse después.
